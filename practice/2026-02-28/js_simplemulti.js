function simpleMultiplication(number) {
    if (number%2==0){number*=8;}
  else{number*=9;}
  return number;
}

function greet(name){
  
  return `Hello, ${name} how are you doing today?`;
  
}